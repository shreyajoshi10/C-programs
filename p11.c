#include <stdio.h>
void main()
{
    int n, i;
    int a[n];
    printf("Enter the number of elements you want to add in the array ");
    scanf("%d", &n);
    for(i=0; i<n; i++)
    {
        printf("Enter the %d element ", i+1);
        scanf("%d", &a[i]);
    }
    for i in a[n]
    {
        printf(a[i])
    }

}
